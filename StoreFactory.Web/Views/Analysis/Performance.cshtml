@{
    ViewData["Title"] = "Performance Analysis";
}

<div class="page-header sf-page-header">
    <h2>Performance Analysis <small>Parallel processing insights</small></h2>
    <p class="text-muted">Benchmark how parallel processing accelerates shrinkage computations.</p>
</div>

<p class="text-muted sf-analysis-intro">
    This page focuses on parallel processing performance using <strong>synthetic_large.csv</strong>.
    For fabric behavior analytics, open <a asp-controller="Analysis" asp-action="Index">Simulation Analysis</a>.
</p>

<p class="text-muted">
    Tip: hover any chart to see exact values. Legends are shown beneath the charts for readability.
</p>

<p class="text-muted sf-analysis-intro">
    Note: Efficiency drops as worker count increases because overhead and contention grow faster than useful parallel work.
</p>

<div class="panel panel-default sf-analysis-summary">
    <div class="panel-heading">Performance summary</div>
    <div class="panel-body">
        <div class="row">
            <div class="col-sm-4">
                <div class="sf-metric">
                    <div class="sf-metric-label">Speedup</div>
                    <div class="sf-metric-value" id="perfSpeedup">-</div>
                    <div class="sf-metric-sub">
                        single: <span id="perfSingle">-</span>s,
                        parallel: <span id="perfParallel">-</span>s
                    </div>
                </div>
            </div>
            <div class="col-sm-4">
                <div class="sf-metric">
                    <div class="sf-metric-label">Rows</div>
                    <div class="sf-metric-value" id="perfRows">-</div>
                    <div class="sf-metric-sub">
                        workers: <span id="perfWorkers">-</span>
                    </div>
                </div>
            </div>
            <div class="col-sm-4">
                <div class="sf-metric">
                    <div class="sf-metric-label">Efficiency (peak)</div>
                    <div class="sf-metric-value" id="perfEfficiency">-</div>
                    <div class="sf-metric-sub">speedup / workers</div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row sf-analysis-grid">
    <div class="col-md-6">
        <div class="panel panel-warning sf-analysis-panel">
            <div class="panel-heading">
                Speedup vs. number of workers
                <span class="sf-help" data-toggle="tooltip" data-placement="left" title="Observed speedup relative to a single worker. The curve flattens when overhead and contention limit scaling.">
                    <img src="~/img/help-icon.svg" alt="Help" class="sf-help-icon" />
                </span>
            </div>
            <div class="panel-body">
                <div class="sf-canvas-wrap">
                    <canvas id="perfSpeedupChart" height="220"></canvas>
                </div>
                <p class="help-block">Measured speedup vs. workers. Linear scaling would be a straight line; flattening implies overhead.</p>
            </div>
        </div>
    </div>
    <div class="col-md-6">
        <div class="panel panel-warning sf-analysis-panel">
            <div class="panel-heading">
                Efficiency vs. workers
                <span class="sf-help" data-toggle="tooltip" data-placement="left" title="Efficiency = speedup / workers. It falls when added workers contribute less due to overhead or memory contention.">
                    <img src="~/img/help-icon.svg" alt="Help" class="sf-help-icon" />
                </span>
            </div>
            <div class="panel-body">
                <div class="sf-canvas-wrap">
                    <canvas id="perfEfficiencyChart" height="220"></canvas>
                </div>
                <p class="help-block">Efficiency falls with more workers when coordination and cache/memory limits dominate.</p>
            </div>
        </div>
    </div>
</div>

<div class="row sf-analysis-grid">
    <div class="col-md-6">
        <div class="panel panel-warning sf-analysis-panel">
            <div class="panel-heading">
                Scaling time (seconds)
                <span class="sf-help" data-toggle="tooltip" data-placement="left" title="Absolute runtime per worker count. Improvements taper off once overhead outweighs extra parallelism.">
                    <img src="~/img/help-icon.svg" alt="Help" class="sf-help-icon" />
                </span>
            </div>
            <div class="panel-body">
                <div class="sf-canvas-wrap">
                    <canvas id="perfScalingChart" height="220"></canvas>
                </div>
                <p class="help-block">Absolute runtime (s) per worker count; diminishing returns show up as a shallow slope.</p>
            </div>
        </div>
    </div>
    <div class="col-md-6">
        <div class="panel panel-warning sf-analysis-panel">
            <div class="panel-heading">
                Parallel overhead estimate
                <span class="sf-help" data-toggle="tooltip" data-placement="left" title="Estimated overhead beyond ideal linear scaling (coordination, scheduling, synchronization, IO). Lower is better.">
                    <img src="~/img/help-icon.svg" alt="Help" class="sf-help-icon" />
                </span>
            </div>
            <div class="panel-body">
                <div class="sf-canvas-wrap">
                    <canvas id="perfOverheadChart" height="220"></canvas>
                </div>
                <p class="help-block">Overhead grows with workers; this limits efficiency and speedup at higher counts.</p>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.4.0/Chart.min.js"></script>
    <script src="~/js/performance.js"></script>
}